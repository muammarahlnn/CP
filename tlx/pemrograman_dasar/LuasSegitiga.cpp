#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); 

    int a, t; cin >> a >> t;
    cout << fixed << showpoint;
    cout << setprecision(2) << (double) a * t / 2 << "\n";
    return 0;
}