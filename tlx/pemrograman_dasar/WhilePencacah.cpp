#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    // cin.tie(NULL); 

    int n, ans = 0;
    while (cin >> n) {
        ans += n;
    }
    cout << ans << "\n";

    return 0;
}